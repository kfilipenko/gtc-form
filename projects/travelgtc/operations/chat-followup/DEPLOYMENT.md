# Публикация 2026-09-25

Статус: PUBLISHED / READY_FOR_OWNER_REVIEW.

- Исходный commit: `194b6aa`; установленный commit: `5e18c49` (одинаковое дерево исходников).
- Сайт: https://travelgtc.com/ и https://travelgtc.com/mira/; общий JS также обновлён для защищённого voice-pilot.
- Резервная копия: `/var/backups/travelgtc/mira-chat-followup-20260925/` — предыдущие source/static под `files/` и полный `dist/`. Список затронутых файлов: `files.json`.
- TypeScript PASS; 154/154 серверных теста PASS.
- 24 браузерные проверки кандидата PASS; ещё 24 на опубликованных HTML/CSS/JS с mock API PASS.
- 9 live HTTP/хеш-проверок PASS: пять файлов, health, создание гостевой сессии без consent, авторизация голосового доступа, пустая собственная история.
- Создана одна пустая синтетическая гостевая сессия для HTTP-проверки; реальные гостевые коды не выдавались. Отдельная PostgreSQL-проверка флагов consent выполнена с ROLLBACK.
- 1 прямой синтетический запрос к Azure PASS; стоимость не измерена. Мира предложила гостевое знакомство вместо нового запроса дат.
- Voice recording на физическом телефоне NOT_RUN; отдельная реальная выдача приглашения NOT_RUN, прежняя схема свежей проверки и ledger не менялась.
- Устаревшее ожидание версии маркетинговой стратегии в auth.test.ts синхронизировано с уже существующей 0.4; сама стратегия не изменена.

Первый перенос Git остановился на каталогах root. Выполнена установка точного набора файлов с повышенными правами и фиксация той же правки в рабочей ветке. API/dist и обе static-копии согласованы, health PASS. Права каталогов и действующие данные CRM не менялись.

Откат: восстановить файлы из `files/` в одноимённые абсолютные пути, вернуть backup/dist в `/var/www/gtc-form/projects/travelgtc/app/dist`, перезапустить `travelgtc-api.service`, проверить `/api/travelgtc/v1/health`. Для новых файлов, отсутствующих в backup, ориентироваться на files.json; не удалять новые данные CRM. Git-откат оформлять отдельным revert-коммитом, без reset истории.

GitHub push и protected-main merge не выполнялись; публикация здесь означает действующий сайт GTC1. Приёмка Owner открыта.
