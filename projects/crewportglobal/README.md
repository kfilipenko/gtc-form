# CrewPortGlobal project scaffold

This directory contains the initial source scaffold for the future CrewPortGlobal.com website.

## Structure

- public/: public-facing pages and publishable Markdown documents.
- public/legal/: legal pages prepared for client-facing publication.
- public/about/: positioning and project context.
- public/how-it-works/: introductory workflow description.
- public/for-shipowners/: shipowner-facing overview.
- public/for-seafarers/: seafarer-facing overview.
- db/migrations/: planning SQL artifacts for isolated CrewPortGlobal schema design.
- deploy/: future nginx and deployment scaffold for GTC1.

## Publishing model

- Markdown is treated as the canonical source for public documentation.
- Public document metadata now lives in YAML frontmatter inside each index.md.
- HTML is used for the landing page and for generated document pages rendered from canonical Markdown.
- Internal source-of-truth planning and governance documents live in docs/crewportglobal/.
- Local preview for directory-style Markdown routes can be served with php -S using public/router.php.

## Document page generation

- Shared document styling lives in public/assets/crewportglobal-docs.css.
- Shared public language runtime lives in public/assets/crewportglobal-public-i18n.js.
- Homepage translations now use the same shared public runtime and provide only page-specific dictionary entries from public/index.html.
- Static HTML pages for public documents are generated by projects/crewportglobal/scripts/generate_public_pages.py.
- The supported entrypoint for reproducible rebuilds is projects/crewportglobal/scripts/run_public_generator.sh.
- The generator reads frontmatter fields such as category, nav_label, summary, trust_note, hero_ctas, summary_cards, acknowledgements and related_links automatically from each document.
- Generated pages are written next to each canonical index.md as index.html so clean routes resolve to HTML while raw Markdown remains available.

## Reproducible generator runtime

- Preferred runtime is the workspace virtualenv at ../.venv relative to the repository root, which resolves to /var/www/.venv in this workspace.
- Install or refresh generator dependencies with: source /var/www/.venv/bin/activate && python -m pip install -r projects/crewportglobal/requirements.txt
- Rebuild public document HTML with: ./projects/crewportglobal/scripts/run_public_generator.sh
- The wrapper prefers an already activated virtualenv, then falls back to ../.venv or .venv if present, and stops with a clear error if Markdown or PyYAML are unavailable.
- If a canonical public Markdown file does not start with valid YAML frontmatter, the generator will fail fast instead of silently emitting partial HTML.

## Onboarding

- Public seafarer acceptance flow lives at public/onboarding/seafarer-registration/index.html.
- The page currently stores acceptance data locally in the browser and does not mutate shared auth state.
- Required no-fee, data-accuracy and Trust Center acknowledgements are captured as real checkbox controls before later identity workstreams are introduced.

## Isolated database planning

- CrewPortGlobal isolated database planning is documented in docs/crewportglobal/22_identity_and_project_database_architecture.md.
- Detailed registration-flow and schema planning is documented in docs/crewportglobal/24_isolated_database_schema_and_registration_flows.md.
- Category-level onboarding gates are documented in docs/crewportglobal/25_category_onboarding_matrix.md.
- Planning SQL lives in projects/crewportglobal/db/migrations/20260510_crewport_initial_schema.sql.
- These files are planning artifacts only and must not be applied to production without a separate approval step.

## Immediate next steps

1. Confirm complaint contact channels and production mailbox ownership.
2. Review the isolated `crewport` schema and registration-flow planning before any server-side implementation work.
3. Re-run the document generator after any public Markdown update.
4. Activate nginx and SSL only after domain and DNS control are confirmed.