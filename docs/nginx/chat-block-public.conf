# Deny public access to /chat/ while keeping /chat/internal/ available.
# Place this after the /chat/internal/ auth block in the server config for app.gtstor.com.

# Internal stays handled by its own location (/chat/internal/) defined elsewhere.

location ^~ /chat/ {
    return 403;
}
