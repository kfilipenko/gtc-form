# <PROJECT_NAME> - Project Memory Handoff

- Project: <PROJECT_NAME>
- Version: 0.1
- Date: <DATE>
- Status: Active memory draft

## 1. Purpose

This document preserves continuation context for `<PROJECT_NAME>`.

It should let a future Project Owner / AI agent session resume without rereading the entire chat history.

## 2. Current Product Direction

```text
<current product direction>
```

## 3. Canonical Sources

| Area | Document / path |
|---|---|
| Register | `docs/<PROJECT_CODE>/00_documentation_register.md` |
| Scope | `docs/<PROJECT_CODE>/01_project_scope_and_positioning.md` |
| Publication | `docs/<PROJECT_CODE>/04_publication_model.md` |
| Business processes | `docs/<PROJECT_CODE>/business_processes/` |
| Implemented standards | `docs/<PROJECT_CODE>/implemented_code_standards/` |

## 4. Current Implementation State

1. `<state item>`;
2. `<state item>`;
3. `<state item>`.

## 5. Accepted Standards And Rules

1. `<standard/rule>`;
2. `<standard/rule>`;
3. `<standard/rule>`.

## 6. Current Public URLs

| Route | Purpose |
|---|---|
| `<PUBLIC_BASE_URL>/` | Public entry |

## 7. Known Gaps / Next Work

| Priority | Task | Reason |
|---|---|---|
| P1 | `<task>` | `<reason>` |

## 8. Active Working Rules

1. Read this memory before implementation.
2. Check the documentation register before creating new docs.
3. Preserve one public URL per public document.
4. Use existing standards before inventing new local patterns.
5. Run verification and commit completed changes.

## 9. Revision History

| Version | Date | Author | Changes |
|---|---|---|---|
| 0.1 | <DATE> | GTC IT / AI Assistant | Initial project memory handoff |
